'use strict';
import React from 'react';

let Settings = React.createClass({

  render() {
    let { context } = this.props;

    return (
      <div>
        Settings here...
      </div>
    );
  }

});

export default Settings;
